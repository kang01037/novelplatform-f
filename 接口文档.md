# 小说平台接口文档

## 1. 文档说明

本文档详细描述了小说平台的所有后端接口，包括用户、小说、章节、书架、评论和微信登录模块。所有接口均基于RESTful风格设计，使用JSON格式进行数据交互。

## 2. 响应格式

所有接口的响应格式统一为：

```json
{
  "code": 200,
  "message": "success",
  "data": "返回数据"
}
```

- `code`: 响应状态码（Integer），200表示成功，其他表示失败
- `message`: 响应消息（String），描述操作结果
- `data`: 响应数据，根据接口不同返回不同格式的数据

## 3. 用户模块

### 3.1 接口列表

| 接口名 | URL | 方法 | 功能描述 |
|-------|-----|------|----------|
| 获取用户信息 | /api/user/{userId} | GET | 根据用户ID获取用户信息 |
| 根据用户名获取用户 | /api/user/username/{username} | GET | 根据用户名获取用户信息 |
| 获取所有用户 | /api/user/list | GET | 获取所有用户列表 |
| 用户注册 | /api/user/register | POST | 用户注册 |
| 更新用户信息 | /api/user/update | PUT | 更新用户信息 |
| 删除用户 | /api/user/delete/{userId} | DELETE | 根据用户ID删除用户 |
| 用户登录 | /api/user/login | POST | 用户登录 |
| 上传头像 | /api/user/upload/avatar | POST | 上传用户头像 |
| 获取头像 | /api/user/avatar/{filename} | GET | 获取用户头像 |
| 删除头像 | /api/user/avatar/{userId} | DELETE | 删除用户头像 |

### 3.2 详细接口

#### 3.2.1 获取用户信息

- **URL**: `/api/user/{userId}`
- **方法**: `GET`
- **参数**: 
  - `userId` (路径参数, Long): 用户ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": {
      "userId": 1,
      "username": "user1",
      "password": "******",
      "email": "user1@example.com",
      "phone": "13800138000",
      "avatar": "avatar_url",
      "nickname": "用户1",
      "wechatOpenid": "wx_12345678",
      "gender": 1,
      "birthday": "1990-01-01",
      "userStatus": 1,
      "lastLoginTime": "2023-01-01T12:00:00",
      "loginCount": 10,
      "createdTime": "2023-01-01T00:00:00",
      "updatedTime": "2023-01-01T00:00:00",
      "deleted": 0
    }
  }
  ```

#### 3.2.2 根据用户名获取用户

- **URL**: `/api/user/username/{username}`
- **方法**: `GET`
- **参数**: 
  - `username` (路径参数, String): 用户名
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": {
      "userId": 1,
      "username": "user1",
      "password": "******",
      "email": "user1@example.com",
      "phone": "13800138000",
      "avatar": "avatar_url",
      "nickname": "用户1",
      "wechatOpenid": "wx_12345678",
      "gender": 1,
      "birthday": "1990-01-01",
      "userStatus": 1,
      "lastLoginTime": "2023-01-01T12:00:00",
      "loginCount": 10,
      "createdTime": "2023-01-01T00:00:00",
      "updatedTime": "2023-01-01T00:00:00",
      "deleted": 0
    }
  }
  ```

#### 3.2.3 获取所有用户

- **URL**: `/api/user/list`
- **方法**: `GET`
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": [
      {
        "userId": 1,
        "username": "user1",
        "password": "******",
        "email": "user1@example.com",
        "phone": "13800138000",
        "avatar": "avatar_url",
        "nickname": "用户1",
        "wechatOpenid": "wx_12345678",
        "gender": 1,
        "birthday": "1990-01-01",
        "userStatus": 1,
        "lastLoginTime": "2023-01-01T12:00:00",
        "loginCount": 10,
        "createdTime": "2023-01-01T00:00:00",
        "updatedTime": "2023-01-01T00:00:00",
        "deleted": 0
      }
    ]
  }
  ```

#### 3.2.4 用户注册

- **URL**: `/api/user/register`
- **方法**: `POST`
- **请求体**: 
  ```json
  {
    "username": "user1",
    "password": "password123",
    "email": "user1@example.com",
    "phone": "13800138000",
    "nickname": "用户1",
    "gender": 1,
    "birthday": "1990-01-01"
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "注册成功"
  }
  ```

#### 3.2.5 更新用户信息

- **URL**: `/api/user/update`
- **方法**: `PUT`
- **请求体**: 
  ```json
  {
    "userId": 1,
    "username": "user1",
    "password": "newpassword123",
    "email": "user1@example.com",
    "phone": "13800138000",
    "avatar": "new_avatar_url",
    "nickname": "用户1",
    "gender": 1,
    "birthday": "1990-01-01"
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "更新成功"
  }
  ```

#### 3.2.6 删除用户

- **URL**: `/api/user/delete/{userId}`
- **方法**: `DELETE`
- **参数**: 
  - `userId` (路径参数, Long): 用户ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "删除成功"
  }
  ```

#### 3.2.7 用户登录

- **URL**: `/api/user/login`
- **方法**: `POST`
- **请求体**: 
  ```json
  {
    "username": "user1",
    "password": "password123"
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "登录成功",
    "data": {
      "token": "JWT token字符串",
      "userInfo": {
        "userId": 1,
        "username": "user1",
        "password": "******",
        "email": "user1@example.com",
        "phone": "13800138000",
        "avatar": "avatar_url",
        "nickname": "用户1",
        "wechatOpenid": null,
        "gender": 1,
        "birthday": "1990-01-01",
        "userStatus": 1,
        "lastLoginTime": "2023-01-01T12:00:00",
        "loginCount": 10,
        "createdTime": "2023-01-01T00:00:00",
        "updatedTime": "2023-01-01T00:00:00",
        "deleted": 0
      }
    }
  }
  ```

#### 3.2.8 上传头像

- **URL**: `/api/user/upload/avatar`
- **方法**: `POST`
- **Content-Type**: `multipart/form-data`
- **参数**: 
  - `userId` (表单参数, Long): 用户ID
  - `file` (文件参数, MultipartFile): 头像文件
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "头像上传成功",
    "data": "头像URL"
  }
  ```

#### 3.2.9 获取头像

- **URL**: `/api/user/avatar/{filename}`
- **方法**: `GET`
- **参数**: 
  - `filename` (路径参数, String): 头像文件名
- **响应**: 图片二进制数据（Content-Type: image/jpeg）

#### 3.2.10 删除头像

- **URL**: `/api/user/avatar/{userId}`
- **方法**: `DELETE`
- **参数**: 
  - `userId` (路径参数, Long): 用户ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "头像删除成功"
  }
  ```

## 4. 小说模块

### 4.1 接口列表

| 接口名 | URL | 方法 | 功能描述 |
|-------|-----|------|----------|
| 获取小说信息 | /api/novel/{novelId} | GET | 根据小说ID获取小说信息 |
| 获取所有小说 | /api/novel/list | GET | 获取所有小说列表 |
| 根据作者ID获取小说 | /api/novel/author/{authorId} | GET | 根据作者ID获取小说列表 |
| 根据分类ID获取小说 | /api/novel/category/{categoryId} | GET | 根据分类ID获取小说列表 |
| 创建小说 | /api/novel/create | POST | 创建新小说 |
| 更新小说 | /api/novel/update | PUT | 更新小说信息 |
| 删除小说 | /api/novel/delete/{novelId} | DELETE | 根据小说ID删除小说 |
| 增加点击量 | /api/novel/{novelId}/click | POST | 增加小说点击量 |
| 增加收藏量 | /api/novel/{novelId}/collect | POST | 增加小说收藏量 |
| 增加推荐量 | /api/novel/{novelId}/recommend | POST | 增加小说推荐量 |
| 评分小说 | /api/novel/{novelId}/rate | POST | 为小说评分 |
| 获取热门小说 | /api/novel/hot | GET | 获取热门小说列表 |
| 获取最新小说 | /api/novel/latest | GET | 获取最新小说列表 |
| 搜索小说 | /api/novel/search | GET | 根据小说名称搜索小说 |
| 上传封面 | /api/novel/upload/cover | POST | 上传小说封面 |
| 获取封面 | /api/novel/cover/{filename} | GET | 获取小说封面 |
| 获取Base64封面 | /api/novel/cover/base64/{filename} | GET | 获取小说封面Base64编码 |

### 4.2 详细接口

#### 4.2.1 获取小说信息

- **URL**: `/api/novel/{novelId}`
- **方法**: `GET`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": {
      "novelId": 1,
      "novelName": "小说名称",
      "authorId": 1,
      "categoryId": 1,
      "coverImage": "cover_url",
      "content": "小说简介",
      "novelStatus": 1,
      "clickCount": 100,
      "collectCount": 50,
      "recommendCount": 30,
      "score": 4.5,
      "scoreCount": 20,
      "lastUpdateTime": "2023-01-01T12:00:00",
      "lastChapterId": 10,
      "lastChapterName": "最后一章",
      "status": 1,
      "createdTime": "2023-01-01T00:00:00",
      "updatedTime": "2023-01-01T00:00:00",
      "deleted": 0
    }
  }
  ```

#### 4.2.2 获取所有小说

- **URL**: `/api/novel/list`
- **方法**: `GET`
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": [
      {
        "novelId": 1,
        "novelName": "小说名称",
        "authorId": 1,
        "categoryId": 1,
        "coverImage": "cover_url",
        "content": "小说简介",
        "novelStatus": 1,
        "clickCount": 100,
        "collectCount": 50,
        "recommendCount": 30,
        "score": 4.5,
        "scoreCount": 20,
        "lastUpdateTime": "2023-01-01T12:00:00",
        "lastChapterId": 10,
        "lastChapterName": "最后一章",
        "status": 1,
        "createdTime": "2023-01-01T00:00:00",
        "updatedTime": "2023-01-01T00:00:00",
        "deleted": 0
      }
    ]
  }
  ```

#### 4.2.3 根据作者ID获取小说

- **URL**: `/api/novel/author/{authorId}`
- **方法**: `GET`
- **参数**: 
  - `authorId` (路径参数, Long): 作者ID
- **响应**: 同获取所有小说的data数组格式

#### 4.2.4 根据分类ID获取小说

- **URL**: `/api/novel/category/{categoryId}`
- **方法**: `GET`
- **参数**: 
  - `categoryId` (路径参数, Long): 分类ID
- **响应**: 同获取所有小说的data数组格式

#### 4.2.5 创建小说

- **URL**: `/api/novel/create`
- **方法**: `POST`
- **请求体**: 
  ```json
  {
    "novelName": "小说名称",
    "authorId": 1,
    "categoryId": 1,
    "coverImage": "cover_url",
    "content": "小说简介",
    "novelStatus": 1
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "小说创建成功"
  }
  ```

#### 4.2.6 更新小说

- **URL**: `/api/novel/update`
- **方法**: `PUT`
- **请求体**: 
  ```json
  {
    "novelId": 1,
    "novelName": "小说名称",
    "authorId": 1,
    "categoryId": 1,
    "coverImage": "cover_url",
    "content": "小说简介",
    "novelStatus": 1
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "更新成功"
  }
  ```

#### 4.2.7 删除小说

- **URL**: `/api/novel/delete/{novelId}`
- **方法**: `DELETE`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "删除成功"
  }
  ```

#### 4.2.8 增加点击量

- **URL**: `/api/novel/{novelId}/click`
- **方法**: `POST`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "点击量已更新"
  }
  ```

#### 4.2.9 增加收藏量

- **URL**: `/api/novel/{novelId}/collect`
- **方法**: `POST`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "收藏量已更新"
  }
  ```

#### 4.2.10 增加推荐量

- **URL**: `/api/novel/{novelId}/recommend`
- **方法**: `POST`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "推荐量已更新"
  }
  ```

#### 4.2.11 评分小说

- **URL**: `/api/novel/{novelId}/rate`
- **方法**: `POST`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
- **请求体**: 
  ```json
  {
    "score": 4.5
  }
  ```
  > `score` 类型为 BigDecimal
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "评分成功"
  }
  ```

#### 4.2.12 获取热门小说

- **URL**: `/api/novel/hot`
- **方法**: `GET`
- **参数**: 
  - `limit` (查询参数, Integer, 默认10): 限制数量
- **响应**: 同获取所有小说的data数组格式

#### 4.2.13 获取最新小说

- **URL**: `/api/novel/latest`
- **方法**: `GET`
- **参数**: 
  - `limit` (查询参数, Integer, 默认10): 限制数量
- **响应**: 同获取所有小说的data数组格式

#### 4.2.14 搜索小说

- **URL**: `/api/novel/search`
- **方法**: `GET`
- **参数**: 
  - `novelName` (查询参数, String, 必填): 小说名称
- **响应**: 同获取所有小说的data数组格式

#### 4.2.15 上传封面

- **URL**: `/api/novel/upload/cover`
- **方法**: `POST`
- **Content-Type**: `multipart/form-data`
- **参数**: 
  - `novelId` (表单参数, Long): 小说ID
  - `file` (文件参数, MultipartFile): 封面文件
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "封面上传成功",
    "data": "封面URL"
  }
  ```

#### 4.2.16 获取封面

- **URL**: `/api/novel/cover/{filename}`
- **方法**: `GET`
- **参数**: 
  - `filename` (路径参数, String): 封面文件名
- **响应**: 图片二进制数据（Content-Type: image/jpeg）

#### 4.2.17 获取Base64封面

- **URL**: `/api/novel/cover/base64/{filename}`
- **方法**: `GET`
- **参数**: 
  - `filename` (路径参数, String): 封面文件名
- **响应**: 
  ```json
  {
    "image": "data:image/jpeg;base64,/9j/4AAQ...",
    "filename": "xxx.jpg"
  }
  ```
  > 该接口返回的并非标准 `ResponseMessage` 格式，而是直接返回包含 Base64 编码图片的 Map 对象。
  > 失败时返回 `{"error": "图片不存在"}`，HTTP状态码404。

## 5. 章节模块

### 5.1 接口列表

| 接口名 | URL | 方法 | 功能描述 |
|-------|-----|------|----------|
| 获取章节信息 | /api/chapter/{chapterId} | GET | 根据章节ID获取章节信息 |
| 根据小说ID获取章节列表 | /api/chapter/novel/{novelId} | GET | 根据小说ID获取章节列表 |
| 根据小说ID和章节号获取章节 | /api/chapter/novel/{novelId}/num/{chapterNum} | GET | 根据小说ID和章节号获取章节 |
| 获取最新章节 | /api/chapter/novel/{novelId}/latest | GET | 获取小说的最新章节 |
| 获取章节数量 | /api/chapter/novel/{novelId}/count | GET | 获取小说的章节数量 |
| 创建章节 | /api/chapter/create | POST | 创建新章节 |
| 更新章节 | /api/chapter/update | PUT | 更新章节信息 |
| 删除章节 | /api/chapter/delete/{chapterId} | DELETE | 根据章节ID删除章节 |
| 批量删除章节 | /api/chapter/delete/batch | DELETE | 批量删除章节 |

### 5.2 详细接口

#### 5.2.1 获取章节信息

- **URL**: `/api/chapter/{chapterId}`
- **方法**: `GET`
- **参数**: 
  - `chapterId` (路径参数, Long): 章节ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": {
      "chapterId": 1,
      "novelId": 1,
      "novelName": "小说名称",
      "chapterNum": 1,
      "chapterTitle": "第一章",
      "chapterContent": "章节内容",
      "wordCount": 1000,
      "createTime": "2023-01-01T00:00:00",
      "updateTime": "2023-01-01T00:00:00"
    }
  }
  ```

#### 5.2.2 根据小说ID获取章节列表

- **URL**: `/api/chapter/novel/{novelId}`
- **方法**: `GET`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": [
      {
        "chapterId": 1,
        "novelId": 1,
        "novelName": "小说名称",
        "chapterNum": 1,
        "chapterTitle": "第一章",
        "chapterContent": "章节内容",
        "wordCount": 1000,
        "createTime": "2023-01-01T00:00:00",
        "updateTime": "2023-01-01T00:00:00"
      }
    ]
  }
  ```

#### 5.2.3 根据小说ID和章节号获取章节

- **URL**: `/api/chapter/novel/{novelId}/num/{chapterNum}`
- **方法**: `GET`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
  - `chapterNum` (路径参数, Integer): 章节号
- **响应**: 同获取章节信息格式

#### 5.2.4 获取最新章节

- **URL**: `/api/chapter/novel/{novelId}/latest`
- **方法**: `GET`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
- **响应**: 同获取章节信息格式

#### 5.2.5 获取章节数量

- **URL**: `/api/chapter/novel/{novelId}/count`
- **方法**: `GET`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": 10
  }
  ```

#### 5.2.6 创建章节

- **URL**: `/api/chapter/create`
- **方法**: `POST`
- **请求体**: 
  ```json
  {
    "novelId": 1,
    "novelName": "小说名称",
    "chapterNum": 1,
    "chapterTitle": "第一章",
    "chapterContent": "章节内容"
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "章节创建成功"
  }
  ```

#### 5.2.7 更新章节

- **URL**: `/api/chapter/update`
- **方法**: `PUT`
- **请求体**: 
  ```json
  {
    "chapterId": 1,
    "novelId": 1,
    "novelName": "小说名称",
    "chapterNum": 1,
    "chapterTitle": "第一章",
    "chapterContent": "章节内容"
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "更新成功"
  }
  ```

#### 5.2.8 删除章节

- **URL**: `/api/chapter/delete/{chapterId}`
- **方法**: `DELETE`
- **参数**: 
  - `chapterId` (路径参数, Long): 章节ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "删除成功"
  }
  ```

#### 5.2.9 批量删除章节

- **URL**: `/api/chapter/delete/batch`
- **方法**: `DELETE`
- **请求体**: 
  ```json
  [1, 2, 3]
  ```
  > 请求体为章节ID数组（List<Long>）
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "批量删除成功"
  }
  ```

## 6. 书架模块

### 6.1 接口列表

| 接口名 | URL | 方法 | 功能描述 |
|-------|-----|------|----------|
| 获取书架信息 | /api/bookshelf/{id} | GET | 根据书架ID获取书架信息 |
| 根据用户ID获取书架列表 | /api/bookshelf/user/{userId} | GET | 根据用户ID获取书架列表 |
| 根据用户ID和小说ID获取书架 | /api/bookshelf/user/{userId}/novel/{novelId} | GET | 根据用户ID和小说ID获取书架信息 |
| 检查是否已加入书架 | /api/bookshelf/check | GET | 检查小说是否已加入用户书架 |
| 获取书架数量 | /api/bookshelf/user/{userId}/count | GET | 获取用户书架中的小说数量 |
| 加入书架 | /api/bookshelf/add | POST | 将小说加入书架 |
| 移出书架 | /api/bookshelf/remove | DELETE | 将小说移出书架 |
| 批量移出书架 | /api/bookshelf/remove/batch | DELETE | 批量将小说移出书架 |
| 更新阅读进度 | /api/bookshelf/reading-progress | PUT | 更新阅读进度 |

### 6.2 详细接口

#### 6.2.1 获取书架信息

- **URL**: `/api/bookshelf/{id}`
- **方法**: `GET`
- **参数**: 
  - `id` (路径参数, Long): 书架ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": {
      "id": 1,
      "userId": 1,
      "novelId": 1,
      "lastReadChapterId": 5,
      "lastReadTime": "2023-01-01T12:00:00",
      "createdTime": "2023-01-01T00:00:00",
      "updatedTime": "2023-01-01T12:00:00"
    }
  }
  ```

#### 6.2.2 根据用户ID获取书架列表

- **URL**: `/api/bookshelf/user/{userId}`
- **方法**: `GET`
- **参数**: 
  - `userId` (路径参数, Long): 用户ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": [
      {
        "id": 1,
        "userId": 1,
        "novelId": 1,
        "lastReadChapterId": 5,
        "lastReadTime": "2023-01-01T12:00:00",
        "createdTime": "2023-01-01T00:00:00",
        "updatedTime": "2023-01-01T12:00:00"
      }
    ]
  }
  ```

#### 6.2.3 根据用户ID和小说ID获取书架

- **URL**: `/api/bookshelf/user/{userId}/novel/{novelId}`
- **方法**: `GET`
- **参数**: 
  - `userId` (路径参数, Long): 用户ID
  - `novelId` (路径参数, Long): 小说ID
- **响应**: 同获取书架信息格式

#### 6.2.4 检查是否已加入书架

- **URL**: `/api/bookshelf/check`
- **方法**: `GET`
- **参数**: 
  - `userId` (查询参数, Long, 必填): 用户ID
  - `novelId` (查询参数, Long, 必填): 小说ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": true
  }
  ```

#### 6.2.5 获取书架数量

- **URL**: `/api/bookshelf/user/{userId}/count`
- **方法**: `GET`
- **参数**: 
  - `userId` (路径参数, Long): 用户ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": 5
  }
  ```

#### 6.2.6 加入书架

- **URL**: `/api/bookshelf/add`
- **方法**: `POST`
- **请求体**: 
  ```json
  {
    "userId": 1,
    "novelId": 1
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "加入书架成功",
    "data": "加入书架成功"
  }
  ```

#### 6.2.7 移出书架

- **URL**: `/api/bookshelf/remove`
- **方法**: `DELETE`
- **请求体**: 
  ```json
  {
    "userId": 1,
    "novelId": 1
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "移出书架成功",
    "data": "移出书架成功"
  }
  ```

#### 6.2.8 批量移出书架

- **URL**: `/api/bookshelf/remove/batch`
- **方法**: `DELETE`
- **请求体**: 
  ```json
  {
    "userId": 1,
    "novelIds": [1, 2, 3]
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "批量移出书架成功",
    "data": "批量移出书架成功"
  }
  ```

#### 6.2.9 更新阅读进度

- **URL**: `/api/bookshelf/reading-progress`
- **方法**: `PUT`
- **请求体**: 
  ```json
  {
    "userId": 1,
    "novelId": 1,
    "chapterId": 5
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "阅读进度已更新",
    "data": "阅读进度已更新"
  }
  ```

## 7. 评论模块

### 7.1 接口列表

| 接口名 | URL | 方法 | 功能描述 |
|-------|-----|------|----------|
| 获取评论信息 | /api/comment/{commentId} | GET | 根据评论ID获取评论信息 |
| 根据小说ID获取评论 | /api/comment/novel/{novelId} | GET | 根据小说ID获取评论列表 |
| 根据章节ID获取评论 | /api/comment/chapter/{chapterId} | GET | 根据章节ID获取评论列表 |
| 根据用户ID获取评论 | /api/comment/user/{userId} | GET | 根据用户ID获取评论列表 |
| 获取评论回复 | /api/comment/replies/{parentId} | GET | 获取评论的回复列表 |
| 获取小说评论数量 | /api/comment/novel/{novelId}/count | GET | 获取小说的评论数量 |
| 获取用户评论数量 | /api/comment/user/{userId}/count | GET | 获取用户的评论数量 |
| 创建评论 | /api/comment/create | POST | 创建新评论 |
| 更新评论 | /api/comment/update | PUT | 更新评论信息 |
| 删除评论 | /api/comment/delete/{commentId} | DELETE | 根据评论ID删除评论 |
| 批量删除评论 | /api/comment/delete/batch | DELETE | 批量删除评论 |
| 点赞评论 | /api/comment/{commentId}/like | POST | 点赞评论 |
| 取消点赞 | /api/comment/{commentId}/unlike | POST | 取消点赞评论 |
| 获取所有评论 | /api/comment/all | GET | 获取所有评论（分页） |

### 7.2 详细接口

#### 7.2.1 获取评论信息

- **URL**: `/api/comment/{commentId}`
- **方法**: `GET`
- **参数**: 
  - `commentId` (路径参数, Long): 评论ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": {
      "commentId": 1,
      "userId": 1,
      "novelId": 1,
      "chapterId": 1,
      "parentId": null,
      "content": "评论内容",
      "likeCount": 10,
      "replyCount": 5,
      "status": 1,
      "createdTime": "2023-01-01T00:00:00",
      "updatedTime": "2023-01-01T00:00:00",
      "deleted": 0
    }
  }
  ```

#### 7.2.2 根据小说ID获取评论

- **URL**: `/api/comment/novel/{novelId}`
- **方法**: `GET`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
  - `page` (查询参数, Integer, 默认1): 页码
  - `size` (查询参数, Integer, 默认20): 每页数量
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": [
      {
        "commentId": 1,
        "userId": 1,
        "novelId": 1,
        "chapterId": 1,
        "parentId": null,
        "content": "评论内容",
        "likeCount": 10,
        "replyCount": 5,
        "status": 1,
        "createdTime": "2023-01-01T00:00:00",
        "updatedTime": "2023-01-01T00:00:00",
        "deleted": 0
      }
    ]
  }
  ```

#### 7.2.3 根据章节ID获取评论

- **URL**: `/api/comment/chapter/{chapterId}`
- **方法**: `GET`
- **参数**: 
  - `chapterId` (路径参数, Long): 章节ID
  - `page` (查询参数, Integer, 默认1): 页码
  - `size` (查询参数, Integer, 默认20): 每页数量
- **响应**: 同根据小说ID获取评论的data数组格式

#### 7.2.4 根据用户ID获取评论

- **URL**: `/api/comment/user/{userId}`
- **方法**: `GET`
- **参数**: 
  - `userId` (路径参数, Long): 用户ID
  - `page` (查询参数, Integer, 默认1): 页码
  - `size` (查询参数, Integer, 默认20): 每页数量
- **响应**: 同根据小说ID获取评论的data数组格式

#### 7.2.5 获取评论回复

- **URL**: `/api/comment/replies/{parentId}`
- **方法**: `GET`
- **参数**: 
  - `parentId` (路径参数, Long): 父评论ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": [
      {
        "commentId": 2,
        "userId": 2,
        "novelId": 1,
        "chapterId": 1,
        "parentId": 1,
        "content": "回复内容",
        "likeCount": 5,
        "replyCount": 0,
        "status": 1,
        "createdTime": "2023-01-01T00:00:00",
        "updatedTime": "2023-01-01T00:00:00",
        "deleted": 0
      }
    ]
  }
  ```

#### 7.2.6 获取小说评论数量

- **URL**: `/api/comment/novel/{novelId}/count`
- **方法**: `GET`
- **参数**: 
  - `novelId` (路径参数, Long): 小说ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": 50
  }
  ```

#### 7.2.7 获取用户评论数量

- **URL**: `/api/comment/user/{userId}/count`
- **方法**: `GET`
- **参数**: 
  - `userId` (路径参数, Long): 用户ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": 10
  }
  ```

#### 7.2.8 创建评论

- **URL**: `/api/comment/create`
- **方法**: `POST`
- **请求体**: 
  ```json
  {
    "userId": 1,
    "novelId": 1,
    "chapterId": 1,
    "parentId": null,
    "content": "评论内容"
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "评论成功"
  }
  ```

#### 7.2.9 更新评论

- **URL**: `/api/comment/update`
- **方法**: `PUT`
- **请求体**: 
  ```json
  {
    "commentId": 1,
    "content": "更新后的评论内容"
  }
  ```
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "更新成功"
  }
  ```

#### 7.2.10 删除评论

- **URL**: `/api/comment/delete/{commentId}`
- **方法**: `DELETE`
- **参数**: 
  - `commentId` (路径参数, Long): 评论ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "删除成功"
  }
  ```

#### 7.2.11 批量删除评论

- **URL**: `/api/comment/delete/batch`
- **方法**: `DELETE`
- **请求体**: 
  ```json
  [1, 2, 3]
  ```
  > 请求体为评论ID数组（List<Long>）
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "批量删除成功"
  }
  ```

#### 7.2.12 点赞评论

- **URL**: `/api/comment/{commentId}/like`
- **方法**: `POST`
- **参数**: 
  - `commentId` (路径参数, Long): 评论ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "点赞成功"
  }
  ```

#### 7.2.13 取消点赞

- **URL**: `/api/comment/{commentId}/unlike`
- **方法**: `POST`
- **参数**: 
  - `commentId` (路径参数, Long): 评论ID
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "success",
    "data": "取消点赞成功"
  }
  ```

#### 7.2.14 获取所有评论

- **URL**: `/api/comment/all`
- **方法**: `GET`
- **参数**: 
  - `page` (查询参数, Integer, 默认1): 页码
  - `size` (查询参数, Integer, 默认20): 每页数量
- **响应**: 同根据小说ID获取评论的data数组格式

## 8. 微信登录模块

### 8.1 接口列表

| 接口名 | URL | 方法 | 功能描述 |
|-------|-----|------|----------|
| 微信登录 | /api/wechat/login | POST | 微信小程序登录 |
| 检查Token | /api/wechat/check-token | POST | 检查Token有效性 |

### 8.2 详细接口

#### 8.2.1 微信登录

- **URL**: `/api/wechat/login`
- **方法**: `POST`
- **请求体**: 
  ```json
  {
    "code": "微信登录码",
    "nickname": "微信昵称",
    "avatarUrl": "微信头像URL"
  }
  ```
  > `code` 为必填，`nickname` 和 `avatarUrl` 为选填
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "登录成功",
    "data": {
      "token": "JWT token字符串",
      "userInfo": {
        "userId": 1,
        "username": "wx_12345678",
        "password": null,
        "email": null,
        "phone": null,
        "avatar": "微信头像URL",
        "nickname": "微信用户",
        "wechatOpenid": "oXXXXXXXXXXX",
        "gender": null,
        "birthday": null,
        "userStatus": 1,
        "lastLoginTime": null,
        "loginCount": null,
        "createdTime": "2023-01-01T00:00:00",
        "updatedTime": "2023-01-01T00:00:00",
        "deleted": 0
      }
    }
  }
  ```

#### 8.2.2 检查Token

- **URL**: `/api/wechat/check-token`
- **方法**: `POST`
- **请求头**: 
  - `Authorization`: Bearer {token}
- **响应**: 
  ```json
  {
    "code": 200,
    "message": "Token有效",
    "data": {
      "userId": 1,
      "username": "wx_12345678",
      "nickname": "微信用户",
      "avatar": "微信头像URL",
      "userStatus": 1
    }
  }
  ```

## 9. 数据结构

### 9.1 用户实体 (User)

| 字段名 | 类型 | 描述 |
|-------|------|------|
| userId | Long | 用户ID |
| username | String | 用户名 |
| password | String | 密码 |
| email | String | 邮箱 |
| phone | String | 手机号 |
| avatar | String | 头像URL |
| nickname | String | 昵称 |
| wechatOpenid | String | 微信OpenID |
| gender | Integer | 性别 |
| birthday | LocalDate | 生日 |
| userStatus | Integer | 用户状态 |
| lastLoginTime | LocalDateTime | 最后登录时间 |
| loginCount | Integer | 登录次数 |
| createdTime | LocalDateTime | 创建时间 |
| updatedTime | LocalDateTime | 更新时间 |
| deleted | Integer | 删除状态 |

### 9.2 小说实体 (Novel)

| 字段名 | 类型 | 描述 |
|-------|------|------|
| novelId | Long | 小说ID |
| novelName | String | 小说名称 |
| authorId | Long | 作者ID |
| categoryId | Long | 分类ID |
| coverImage | String | 封面图片URL |
| content | String | 小说简介 |
| novelStatus | Integer | 小说状态 |
| clickCount | Long | 点击量 |
| collectCount | Long | 收藏量 |
| recommendCount | Long | 推荐量 |
| score | BigDecimal | 评分 |
| scoreCount | Integer | 评分次数 |
| lastUpdateTime | LocalDateTime | 最后更新时间 |
| lastChapterId | Long | 最后章节ID |
| lastChapterName | String | 最后章节名称 |
| status | Integer | 状态 |
| createdTime | LocalDateTime | 创建时间 |
| updatedTime | LocalDateTime | 更新时间 |
| deleted | Integer | 删除状态 |

### 9.3 章节实体 (Chapter)

| 字段名 | 类型 | 描述 |
|-------|------|------|
| chapterId | Long | 章节ID |
| novelId | Long | 小说ID |
| novelName | String | 小说名称 |
| chapterNum | Integer | 章节号 |
| chapterTitle | String | 章节标题 |
| chapterContent | String | 章节内容 |
| wordCount | Integer | 字数 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |

### 9.4 书架实体 (Bookshelf)

| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | Long | 书架ID |
| userId | Long | 用户ID |
| novelId | Long | 小说ID |
| lastReadChapterId | Long | 最后阅读章节ID |
| lastReadTime | LocalDateTime | 最后阅读时间 |
| createdTime | LocalDateTime | 创建时间 |
| updatedTime | LocalDateTime | 更新时间 |

### 9.5 评论实体 (Comment)

| 字段名 | 类型 | 描述 |
|-------|------|------|
| commentId | Long | 评论ID |
| userId | Long | 用户ID |
| novelId | Long | 小说ID |
| chapterId | Long | 章节ID |
| parentId | Long | 父评论ID |
| content | String | 评论内容 |
| likeCount | Integer | 点赞数 |
| replyCount | Integer | 回复数 |
| status | Integer | 状态 |
| createdTime | LocalDateTime | 创建时间 |
| updatedTime | LocalDateTime | 更新时间 |
| deleted | Integer | 删除状态 |
